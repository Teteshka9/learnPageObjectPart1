# learnPageObjectPart1

##### Here im using Java + Selenium to practice automation via demoqa website.
##### This part1 is about how to driver.get(URL) -> using explicit wait through WebDriverWait and ExpectedConditions.
##### Turning on JavascriptExecutor to executeScript using ("arguments[0].focus();", element) and not just simple findElement(By.id()); + scrollIntoView(true), element); 
##### Part1 is ON, many more to come. 

### sincere, Teteshka.

